@model bool

@if (Model)
{
    <div class="row">
        <div class="col-12">
            <div class="alert alert-warning mb-3" role="alert" aria-live="polite">
                <h6>@UiText["notice.restrictedOperations.title"]</h6>
                <p class="small mt-1 mb-0">
                    @UiText["notice.restrictedOperations.description"]
                </p>
            </div>
        </div>
    </div>
}
